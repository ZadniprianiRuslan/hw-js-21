// const numbers = [1, 2, 3, 4, 5, 6];
// console.log(_.chunk(numbers, 2));

// =====================================================================================================

// const numbers = [1, 2, 3, 4, 5, 6];
// console.log(_.drop(numbers, 2));
// console.log(_.drop(numbers, 5));

// =====================================================================================================

// const numbers = [1, 2, 3, 4, 5, 6];
// console.log(_.head(numbers));

// =====================================================================================================

// const numbers = [1, 2, 3, 4, 5, 6];
// console.log(_.last(numbers));

// =====================================================================================================

// const numbers = [1, 2, 3, 4, 5, 6];
// console.log(_.reverse(numbers));

// =====================================================================================================

// const numbers = [1, 2, 3, 4, 5, 6];
// console.log(_.without(numbers, 2, 5));

// =====================================================================================================

// const vars = {
//   a: {
//     b: 2,
//   },
// };
// const otherVars = _.create({ a: _.create({ b: 2 }) });

// console.log(_.has(vars, "a"));
// // => true

// console.log(_.has(vars, "a.b"));
// // => true

// console.log(_.has(vars, ["a", "b"]));
// // => true

// console.log(_.has(otherVars, "a"));
// // => false

// =====================================================================================================

// const vars = {
//   a: 1,
//   b: "2",
//   c: 3,
// };

// console.log(_.pick(object, ["a", "c"]));
// // => { 'a': 1, 'c': 3 }

// =====================================================================================================

// const vars = {
//   a: 1,
//   b: "2",
//   c: 3,
// };

// console.log(_.omit(vars, ["a", "c"]));
// // => { 'b': '2' }

// =====================================================================================================

// const vars = {
//   a: [{ b: 2 }, { d: 4 }],
// };

// const otherVars = {
//   a: [{ c: 3 }, { e: 5 }],
// };

// console.log(_.merge(vars, otherVars));
// // => { 'a': [{ 'b': 2, 'c': 3 }, { 'd': 4, 'e': 5 }] }
